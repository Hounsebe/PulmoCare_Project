from administration.models import Utilisateur

# Remplace 'admin' par ton vrai pseudo
user = Utilisateur.objects.get(username='ton_pseudo')

# On lui redonne tous les super-pouvoirs
user.is_staff = True
user.is_superuser = True
user.is_active = True

# Très important : On lui assigne le service ADMIN pour ton architecture SOA
user.service = 'ADMIN' 

user.save()
exit()